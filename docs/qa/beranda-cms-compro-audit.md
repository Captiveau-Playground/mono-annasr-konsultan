# QA Beranda: CMS ↔ Compro — Temuan & Status Perbaikan

Sumber: 11 issue QA (`annasr.captiveau.id` vs CMS). Audit & perbaikan: 2026-09-10.
Status `✅ = selesai di kode (belum di-deploy)` · `⏳ = menunggu isi data CMS / deploy`.

| #   | Issue                                                    | Steps Reproduce      | Expected                 | Actual                             | Temuan (Akar Masalah)                                                                         | Apa yang Diperbaiki                                                                                                 | Status |
| --- | -------------------------------------------------------- | -------------------- | ------------------------ | ---------------------------------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ------ |
| 1   | Tampilan icon tidak konsisten                            | Beranda → hero       | icon konsisten           | icon berbeda                       | `Hero.tsx` item keunggulan #1 pakai `<Check>`, #2/#3 pakai dot                                | Semua item keunggulan kini render `<Check>` konsisten                                                               | ✅     |
| 2   | Field keunggulan hero tampil code editor                 | Beranda CMS → hero   | bukan code editor        | tampil sebagai code editor JSON    | Schema `annasr/hero.json`: `keunggulan` type `json`                                           | Ganti ke `text` (textarea) + migrasi data JSON lama → satu baris per poin                                           | ✅     |
| 3   | Logo pada list klien tidak tampil                        | Beranda → klien      | logo tampil              | gambar logo tidak tampil           | URL media `http://strapi:1337/...` (host Docker internal) bocor ke browser                    | Semua `/uploads/...` diarahkan ke proxy same-origin `/api/asset/uploads/...`                                        | ✅     |
| 4   | Section founder tidak sesuai CMS                         | Beranda → founder    | tampil sesuai CMS        | tidak sesuai CMS                   | `FounderSection` abaikan prop CMS, pakai teks & gambar statis; `founder` tak punya field foto | Founder sekarang bind nama/jabatan/teks/foto dari Beranda CMS (fallback statis) + field `foto` baru                 | ✅     |
| 5   | Keunggulan bisa di-manage di CMS bagian mana?            | Beranda → keunggulan | ada field di CMS Beranda | tidak ada field                    | Section "Mengapa Memilih" ambil dari Tentang, Beranda tak punya field                         | Tambah field `keunggulan` (repeatable) di Beranda; KenapaKami kini ambil dari Beranda                               | ✅ ⏳  |
| 6   | Layangan ambil dari beranda atau menu layanan?           | Beranda → layanan    | perlu konfirmasi         | ambil dari single type Layanan     | `page.tsx` pakai `kontenSitus.layanan`                                                        | Section layanan kini ambil dari **Beranda.layanan** (satu sumber: Beranda)                                          | ✅ ⏳  |
| 7   | Gambar section proyek tidak tampil                       | Beranda → proyek     | gambar tampil            | gambar tidak tampil                | `/_next/image?url=http://strapi:1337/...` → HTTP 400                                          | Perbaikan URL media global (sama dengan #3)                                                                         | ✅     |
| 8   | Proyek ambil dari portfolio beranda atau menu portfolio? | Beranda → proyek     | perlu konfirmasi         | ambil dari field portfolio Beranda | Sudah benar (Beranda.portfolio)                                                               | Dikonfirmasi tetap Beranda.portfolio sebagai sumber                                                                 | ✅     |
| 9   | Jangkauan proyek beda dengan CMS                         | Beranda → jangkauan  | sesuai CMS               | tidak sesuai                       | Ambil dari Tentang (`tentang.kotaProyek/jangkauan*`); kartu stat hardcoded                    | Kini ambil **Beranda.kotaProyek** + field baru `jangkauanJudul/Deskripsi`; kartu statistik dari `beranda.statistik` | ✅ ⏳  |
| 10  | Artikel ambil dari mana? tidak ada di Beranda            | Beranda → artikel    | ada field di CMS Beranda | tidak ada field                    | Ambil dari single type Artikel; Beranda tak punya field                                       | Tambah field `artikel` (repeatable) di Beranda; section artikel kini ambil dari Beranda                             | ✅ ⏳  |
| 11  | Field FAQ diberi title                                   | CMS Beranda → FAQ    | ada title tiap item      | tidak ada title                    | Strapi pakai atribut `string` pertama sbg judul; `tanya` bertipe `text`                       | Ubah `tanya` → `string` (+ migrasi kolom) → judul accordion = pertanyaan                                            | ✅     |

**Catatan:**

- Perubahan schema + migrasi: `docs/qa` ini menyertakan file migrasi di `apps/strapi/database/migrations/` (`20260910_faq_tanya_text_to_string.js`, `20260910_hero_keunggulan_json_to_text.js`).
- `⏳` = setelah deploy, admin perlu mengisi field baru (Keunggulan, Artikel, Jangkauan, foto Founder); selama kosong system pakai fallback statis.
- Verifikasi: `pnpm typecheck` (UI ✅), `pnpm test` — 72/72 lulus.
